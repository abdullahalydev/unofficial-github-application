{
  "name": "unofficial-github-desktop-application",
  "description": "themy is platform serve creative theme for discord profiles and designs",
  "version": "1.0.0",
  "main": "index.js",
  "dependencies": {
    "electron": "^25.2.0",
    "electron-packager": "^17.1.1",
    "typescript": "^5.1.6"
  },
  "scripts": {
    "build": "npx electron-packager . github-application --overwrite --asar --arch=ia32 --prune=true --out=build --icon=github.ico",
    "build:win32": "npm run build --platform=win32",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC"
}
